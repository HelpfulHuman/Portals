const { expect } = require('chai');
const { interceptors: { mergeGlobals } } = require('../../');

describe('interceptors/mergeGlobals', function () {

  it('performs a shallow merge of the top level values');

  it('performs a shallow merge for the header values');

});
