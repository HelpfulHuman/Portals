const { expect } = require('chai');
const { utils: { formatResponse } } = require('../../');

describe('utils/formatResponse', function () {

  it('converts the given XHR object into a simple object literal with status, body, type, and standard headers');

});
